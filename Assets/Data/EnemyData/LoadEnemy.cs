using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadEnemy : MonoBehaviour
{
    /*
    void Start()
    {
        var enemy = Resources.Load<GameObject>("Prefabs/Enemy");
        var material_2 = Resources.Load<Material>("Materials/Lemon");

        var enemyInstantiate = Instantiate(enemy); // .GetComponent<Enemy>();
        // enemyInstantiate.SetEnemyPosition(new Vector3(-5, 1, 9));

        var enemyInstantiate2 = Instantiate(enemy);
        // enemyInstantiate2.SetEnemyPosition(new Vector3(-20, 1, 1));
        enemyInstantiate2.name = "Enemy_2";
        var renderers = enemy.GetComponentsInChildren<Renderer>();
        foreach (var render in renderers)
        {
            render.material = material_2;
        }

        /////////////////////////////////////////////////

        Resources.UnloadUnusedAssets();

    } */
}
